import React, { memo } from 'react';

import styles from './CommentTextarea.module.scss';

function CommentTextarea() {
  return <div className="mb-3">CommentTextarea</div>;
}

export default memo(CommentTextarea);
